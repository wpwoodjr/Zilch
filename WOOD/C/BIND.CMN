/* this struct maps onto common block produced by makebind (hopefully) */

declare struct {
	short int
	  mode,
	  modes[NUM_MODES],
	  keypad,
	  macros[NUM_MACROS],
	  free,
	  bmax;
	char
	  alternate_escape,
	  filler,
	  alternate_escape_followers[128];
	short int
	  mem[1];
	} bind;
